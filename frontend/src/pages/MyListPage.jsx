import { useSelector } from 'react-redux'; import { content } from '../data/content'; import ContentCard from '../components/ContentCard'; import EmptyState from '../components/EmptyState';
export default function MyListPage() { const list = useSelector((state) => state.myList); const items = content.filter((item) => list.includes(item.id)); return <main className="page"><p className="eyebrow">Your saved stories</p><h1>My List</h1>{items.length ? <div className="content-grid">{items.map((item) => <ContentCard key={item.id} item={item} />)}</div> : <EmptyState title="Your list is waiting" message="Save films and shows to watch whenever you’re ready." />}</main>; }
