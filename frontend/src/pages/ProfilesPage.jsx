import { useState } from 'react'; import { Link } from 'react-router-dom';
const initialProfiles = [{ name: 'Jonathan', avatar: 'J', tone: '' }, { name: 'Kids', avatar: '★', tone: 'kids' }];
export default function ProfilesPage() { const [profiles, setProfiles] = useState(initialProfiles); const [managing, setManaging] = useState(false); const addProfile = () => { if (profiles.length < 4) setProfiles([...profiles, { name: `Profile ${profiles.length + 1}`, avatar: '+', tone: 'new' }]); }; return <main className="profiles"><p className="eyebrow">StreamLocal</p><h1>{managing ? 'Manage profiles' : 'Who’s watching?'}</h1><p className="profile-subtitle">{managing ? 'Profile changes are stored locally for this session.' : 'Choose a profile to start watching.'}</p><div className="profile-list">{profiles.map((profile) => <Link to="/" className={`profile-tile ${profile.tone}`} key={profile.name}><b>{profile.avatar}</b><span>{profile.name}</span></Link>)}{profiles.length < 4 && <button className="profile-tile add" onClick={addProfile}><b>+</b><span>Add Profile</span></button>}</div><button className="manage-button" onClick={() => setManaging(!managing)}>{managing ? 'Done' : 'Manage profiles'}</button></main>; }
