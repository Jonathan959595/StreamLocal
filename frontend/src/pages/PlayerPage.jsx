import { Link, useParams } from 'react-router-dom'; import { findContent } from '../data/content'; import EmptyState from '../components/EmptyState';
export default function PlayerPage() { const item = findContent(useParams().id); if (!item) return <main className="page"><EmptyState title="Video unavailable" message="This title is not available in the local catalog." /></main>; return <main className="player-page"><Link to={`/details/${item.id}`} className="player-back">‹ Back to details</Link><section className="player-shell" aria-label={`${item.title} player`}><div className="player-placeholder"><button aria-label="Play unavailable video">▶</button><p>Video unavailable</p><small>The player shell is ready for the local streaming endpoint.</small></div><div className="player-controls" aria-label="Player controls"><button aria-label="Play or pause">▶</button><div className="timeline" aria-label="Playback progress"><i /></div><span>0:00 / {item.duration}</span><button aria-label="Mute or change volume">♬</button><button aria-label="Fullscreen">⛶</button></div></section><div className="player-title"><p className="eyebrow">{item.type} · {item.year}</p><h1>{item.title}</h1><p>{item.description}</p></div></main>; }
