import { useEffect, useState } from 'react'; import ContentCard from '../components/ContentCard'; import EmptyState from '../components/EmptyState'; import { contentService } from '../services/api';
export default function SearchPage() { const [query, setQuery] = useState(''); const [results, setResults] = useState([]); const [loading, setLoading] = useState(false);
  useEffect(() => { const timer = setTimeout(async () => { if (!query.trim()) return setResults([]); setLoading(true); setResults(await contentService.search(query)); setLoading(false); }, 250); return () => clearTimeout(timer); }, [query]);
  const clear = () => { setQuery(''); setResults([]); }; return <main className="page"><p className="eyebrow">Find something great</p><h1>Search</h1><div className="search-wrap"><label className="sr-only" htmlFor="catalog-search">Search the catalog</label><input id="catalog-search" className="search-field" autoFocus value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Titles, genres, movies, series…" />{query && <button className="clear-search" onClick={clear} aria-label="Clear search">×</button>}</div>{loading && <p className="loading-copy">Searching StreamLocal…</p>}{!query && <EmptyState title="What are you in the mood for?" message="Search titles, genres, films, or series from your local catalog." />}{query && !loading && !results.length ? <EmptyState title="No matches found" message="Try a different title, genre, or keyword." /> : query && <><p className="result-count">{results.length} result{results.length === 1 ? '' : 's'} for “{query}”</p><div className="content-grid">{results.map((item) => <ContentCard key={item.id} item={item} />)}</div></>}</main>; }
